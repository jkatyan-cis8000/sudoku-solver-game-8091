# Config Layer

Constants, settings, and environment configuration.

This layer contains all configuration values, constants, and environment settings.

## What belongs here

- Global constants (e.g., GRID_SIZE, DIFFICULTY_LEVELS)
- Environment variables
- Configuration classes
- Default values

## Rules

- May import from: `types`, `config`
- No business logic
- No external dependencies (e.g., database, network)
